{
  "name": "fix-workflows",
  "description": "Analyze and fix failed GitHub Actions workflows",
  "parameters": {
    "run_id": {
      "type": "string",
      "description": "Specific workflow run ID to fix (optional, uses latest failed run if not provided)",
      "required": false
    }
  }
}
